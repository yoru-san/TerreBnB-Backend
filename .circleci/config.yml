version: 0
jobs:
  build:
    docker: # use the docker executor type; machine and macos executors are also supported
      - image: circleci/node:10.15.0-jessie # the primary container, where your job's commands are run
    steps:
      - checkout # check out the code in the project directory
      - run: echo "Updating environment"
      - run: npm install -g typescript
      - run: npm install -g eslint
      - run: npm install -g eslint-plugin-import
      - run: npm install -g eslint-config-airbnb-base
      - run: npm install -g typescript-eslint-parser
      - run: echo "Running application"
      - run: npm run build-ts
      - run: npm start
